# ZJU Forum Search

这是一个运行在 CC98 和朵朵校友圈页面上的 Manifest V3 浏览器扩展。用户输入自然语言后，扩展让 OpenAI 兼容模型规划检索词，再调用当前站点的搜索接口。候选合并和本地预排序都在浏览器中完成。朵朵已接入，用户已运行手动向导并反馈整体体验良好；平台内部机制的已知限制见调查记录。

## 构建和加载

```bash
cd frontend
npm ci
npm test
npm run build
```

构建产物位于 `frontend/dist/`。在 Edge 打开 `edge://extensions`，启用“开发人员模式”，点击“加载解压缩的扩展”，然后选择该目录。在 `https://www.cc98.org/` 或 `https://www.duoduo.link/` 登录并刷新页面，右下角会出现“搜”悬浮球。

扩展复用当前站点登录态，没有账号输入入口。CC98 使用原站短期授权，朵朵使用原站微信扫码登录后的授权。遇到未登录或登录过期时，先在原站完成登录，再刷新页面。

验收 Issue #26 的本地预排序时运行：

```bash
bash frontend/scripts/issue-26-acceptance-wizard.sh
```

向导先运行完整前端检查，再逐步提示 Edge 中需要人工确认的结果分区、增量顺序和卡片内容。它不读取或保存 API key、Cookie、登录态、请求内容或模型响应。

验收 Issue #27 的最终列表重排时运行：

```bash
bash frontend/scripts/issue-27-acceptance-wizard.sh
```

向导先验证 Top-M、白名单、删除保护、失败回退和旧协议清理，再引导检查设置控件、反馈轮独立运行、单次最终列表重排状态和取消回退。它不读取浏览器存储或模型请求，也不会保存认证数据。

验收 Issue #28 的停止、重排取消和新搜索替换时运行：

```bash
bash frontend/scripts/issue-28-acceptance-wizard.sh
```

向导会先验证取消竞态、迟到响应隔离、失败回退、停止优先级和生产构建，再引导人工检查“停止并查看结果”、重排取消和新查询替换。它不会读取或保存认证信息。

验收 Issue #30 的查询组合、可验证扩展和模型元数据边界时运行：

```bash
bash frontend/scripts/issue-30-acceptance-wizard.sh
```

向导先运行完整测试、类型检查和构建。随后在本地检查结果卡片、隐私文案和模型请求中没有 `author` 字段。它不读取或保存 API key、Cookie、认证头、请求内容或模型响应。查询角色、扩展授权、排序稳定性和失败回退由现有自动测试验收。

验收 Issue #31 的无正信号救援、停止门和 CC98 已知分页时运行：

```bash
bash frontend/scripts/issue-31-acceptance-wizard.sh
```

向导先运行完整测试、类型检查、生产构建和扩展产物检查，再引导人工检查真实登录会话中的搜索进度、结果保留和终止文案。空候选、弱信号、请求预留、停止门、短页分页和请求上限由 mock 测试验收；向导不会读取或保存 API key、Cookie、认证头、请求内容或模型响应。

## 模型设置

打开悬浮球，在“模型设置”中填写：

- OpenAI 兼容 base URL，例如 `https://example.com/v1`
- API key
- 模型名称
- 站点检索请求次数上限（默认 30，范围 1–100）
- 单次反馈证据数量（默认 30，范围 10–100）
- 是否在搜索结束后执行最终列表重排（默认开启）
- 最终列表重排 Top-M（默认 30，范围 10–150）

保存时，浏览器只申请 base URL 所在主机的访问权限。API key 保存在 `chrome.storage.local`，该存储并不加密。service worker 用 key 请求 `{base URL}/chat/completions`，但不会把它回传给 content script。key 不会写入站点页面的 light DOM、日志或仓库。

## 搜索边界

CC98 请求间隔为 2 秒，朵朵初始间隔为 500ms；两者默认每页请求 20 条，一次搜索按用户设置最多调用 1 至 100 次站点请求，分页同样计数。朵朵的间隔不是实测安全阈值，遇到验证码或限流会停止。达到请求上限、模型判断停止、没有新证据、用户手动停止或出错后，面板保留已有结果并说明原因。关闭面板不会停止搜索。

“停止并查看结果”只终止当前和后续站点请求。如果已有至少两条主结果候选且开启了最终列表重排，扩展会用独立的取消范围执行一次重排。重排开始后，操作按钮改为“取消最终列表重排”。取消会立即恢复完整的本地预排序，不删除候选，也不改变原有的搜索停止原因。重排超时、失败或返回无效结构时使用相同的本地回退。

再次提交查询会替换整个旧运行。扩展会取消旧运行未完成的站点检索、反馈调用和最终列表重排；旧请求之后返回的成功、失败或取消响应都不会改动新运行的结果和状态。

搜索按波次推进。首轮计划为检索词标记 `precise`、`balanced` 或 `anchor`。标题来源在请求上限允许时，先让模型顺序中最靠前的一个 `anchor` 和一个非 `anchor` 各取一页；查询角色不参与候选排序。组合缺少任一角色时仍执行已有检索词，并在进度区提示。后续波次先执行通过校验的新检索词，再公平推进已有检索词的分页。同一检索词在一个波次中最多请求一页。首次请求和分页共用同一个站点请求上限，也沿用来源的请求间隔、游标保护和时间提前停止规则。

CC98 搜索结果按发布时间倒序返回；当查询带明确起始日期且某页全部结果的发布时间都已知并早于该日期时，扩展停止该检索词的后续翻页，不影响其他检索词。朵朵按热度排序，不做这一提前停止。

搜索正常结束后，扩展默认从主结果候选中取本地预排序前 M 条，发起一次最终列表重排。模型返回相对顺序和明确移除建议。相关性等级 1、2 和 3 只参与排序；只有请求发出前仍未判断的候选可以删除。相关性等级 0、时间范围外候选和 Top-M 外候选不进入请求。Top-M 外候选保持本地顺序。主结果候选为零条或一条、新查询替换旧运行，以及关闭开关时会跳过这次调用。

最终列表重排只发送原始查询、运行内临时键、原生标题、发布时间、板块和回复数。作者留在本地结果卡片中，不进入模型请求。板块可以帮助模型判断查询意图；回复数只在其他相关性信号接近时用于弱破同分，不能单独提高相关性。重排也不发送相关性等级、检索支持、必须概念命中、检索词数量、来源位次、轮次、URL、正文、回帖或认证数据；朵朵正文派生标题发送为空。planner 接收前和最终序列化时都会执行字段白名单。模型超时、网络失败、非法 JSON 或无法识别的响应结构不会改变本地预排序，重复键和未知键会被忽略，遗漏的 Top-M 候选按本地顺序追加。

首轮模型调用会发送用户查询和浏览器当前日期。反馈轮从待判断候选中选择有限证据，一次返回相关性等级、后续检索词和停止判断。每条后续检索词声明 `query` 或 `evidence` 依据。当前运行只执行证据依据扩展；它必须引用本次响应中等级为 2 或 3 的 1 至 3 个候选键，且支持候选必须有原生标题。无效条目单独丢弃。候选按最新独立证据所属检索词轮转，每个检索词最多提供 4 条；未选中的候选仍可进入后续反馈。只有相关性等级 0 会进入默认折叠的软隔离区域，相关性等级 1、2、3 和未判断候选留在主列表。候选被不同检索词再次命中或模型可见元数据变化后可以重判；同一检索词下只有位次变化和只有作者变化都不会触发重判。

浏览器先执行时间硬约束，再对主列表做确定性的本地预排序。相关性等级 3 排在等级 2 前，等级 1 与未判断候选同级。同一等级内依次比较排除词、必须概念、不同检索词命中数、来源最佳位次和首次命中轮次。模型阶段关闭、取消或失败时，界面完整保留这个顺序。

反馈模型只收到运行内临时键、最多 3 个不同检索支持，以及原生标题、发布时间、板块和回复数。作者、URL、平台标识、来源位次、轮次、片段、正文、回帖、认证数据和用户身份都不会进入负载，正文派生标题发送为空。完整负载不超过 4000 UTF-8 字节。板块可以帮助判断相关性，但不能提供标题检索词；回复数不能单独提高相关性等级。所有模型调用都关闭思考模式，将最大输出限制为 1200 tokens，并通过 `AbortController` 在 20 秒后终止。反馈超时、网络错误或无效响应会停止迭代，保留未软隔离结果，并跳过最终列表重排。

扩展会在本地规范化模型计划：缺失的说明字段和空约束使用默认值，字符串形式的检索词转换为标准结构，缺失或未知的查询角色按 `balanced` 处理，空的必须概念会被丢弃。规范化后仍没有可用检索词时，纯单关键词直接按用户原词搜索，并在进度区说明已启用兜底。网络错误和模型超时不会触发原词兜底。

用户明确写出“近三年”“2025 年”“去年”等时间条件时，扩展会在本地校验模型给出的起止日期，并从主结果、软隔离区域和反馈证据中移除确定在范围外的帖子。发布时间未知的帖子继续保留。没有明确时间条件时，扩展不按发布时间过滤。面板分别显示主结果、软隔离结果和时间范围外结果的数量。

## 搜索源边界

CC98 的认证读取、请求、响应解析和分页位于 `frontend/src/extension/sites/cc98/`。界面通过 `source-registry.ts` 按页面地址选择 adapter，再创建搜索 session；界面不接触论坛 token。朵朵 adapter 位于 `frontend/src/extension/sites/duo/`，负责 `ddtk`、加密信封、快照分页与响应映射。manifest 已登记两个站点及其 API 主机，后台按 registry 校验消息来源。

计划缓存和“换一组检索词”入口已按 issue #10 的验收反馈撤回，再次提交查询会重新搜索。SearchSource 接手审查及朵朵接入前的未决项见 [审查记录](docs/research/search-source-handoff-review.md)。

core 将候选对象、每次召回观察与排序文档分开保存。同一帖子被多词命中时，排序会使用所有文档中的本地片段，并从观察记录计算跨词命中和位次。结果卡片不携带内部排序键。

反馈证据在 core 内构造，进入 planner 前已去除 ID、URL、片段和召回记录，并按用户设置限制为 10 至 100 条。实际模型请求再次执行字段白名单和 4000 UTF-8 字节上限。正文派生标题只在本地显示，反馈标题为空；CC98 的原生标题反馈行为不变。

planner 的首轮和反馈提示词由 `SourceCapabilities` 生成。CC98 使用标题匹配与普通关键词，首轮提示会要求一个宽锚点和一个非宽锚点；朵朵使用全文匹配与普通关键词，这一能力仍需登录后对照原站验证。空候选与弱信号救援统一走 Feedback；全文匹配不改变正文不出域规则，反馈缺少原生标题时模型只能依据查询、命中数与允许的元数据继续规划。

`frontend/src/extension/feedback-payload.ts` 集中维护反馈白名单、标题来源检查及条数和字节上限；`text.ts` 提供归一化与大小写折叠函数。搜索源能力通过扩展消息传给后台 planner，后台只接受已声明的能力枚举。

新增 adapter 时，在 `source-registry.ts` 注册，并将其 `pageMatches` 和 `apiHosts` 分别登记到 manifest 的 `content_scripts.matches` 与 `host_permissions`。运行 `cd frontend && npm test -- src/extension/source-manifest.test.ts` 检查所有已注册源的权限是否齐全。测试按声明的模式字符串检查包含关系，允许 manifest 保留额外的模型端点权限；不解析更宽通配符是否等价覆盖。

## 检索波次手动验收

Issue #25 的浏览器验收向导覆盖设置、真实请求顺序、软隔离、反馈负载和朵朵冒烟。它不会读取或保存 API key、登录态、请求内容或模型响应。

```bash
bash frontend/scripts/issue-25-acceptance-wizard.sh
```

不可稳定触发的重判、失败回退和精确负载上限由自动测试验收。向导会明确指出这些边界，不要求为了复现分支高频请求站点或模型。

## 朵朵手动验收

```bash
bash frontend/scripts/issue-18-19-acceptance-wizard.sh
# 只运行本地检查，不打开浏览器、不进行真人验收：
bash frontend/scripts/issue-18-19-acceptance-wizard.sh --check-only
```

向导分 10 阶段，每阶段用 y/N 确认。本人在 Edge 完成登录、搜索、结果跳转与错误提示验收；脚本不读取或保存登录态。无法核实的真实会话项目会列入待验证清单，不会因自动测试通过就视作已验收。

朵朵显示标题由正文截断生成，只供本地展示；模型反馈标题为空。扩展默认省略 `group_id`，不读取用户资料、不硬编码学校编号，也不继承原站的圈层筛选。搜索范围、`ddtk` 与 `duo_session` 的关系、未登录空结果含义、`needCode` 条件及阈值均需本人会话确认。协议与实现取舍见 [API 调研](docs/research/duo-alumni-api-survey.md) 和 [实施记录](docs/experiments/issue-18-19-duo-adapter.md)。

本轮交付复核见 [#12–19 结案记录](docs/experiments/issues-12-19-closeout.md)。平台内部认证标识关系、空结果原因及风控阈值等未知项由开发侧保留，用户无需为日常使用验收查看凭据或主动触发风控。
