# CC98 自然语言搜索

本项目面向浙江大学学生，帮助用户在本人可访问的 CC98 与朵朵校友圈内容范围内，用自然语言定位相关帖子。

**朵朵帖子**：朵朵校友圈中以 sns_id 标识的内容对象，没有独立的原生标题。正文派生的显示标题只供本地展示，不能发送给模型。

**圈层筛选**：朵朵按院校或群组限定检索范围的条件，以 group_id 表示。扩展不自动继承原站的圈层筛选。下文以 CC98 起步阶段的术语描述搜索行为；迭代搜索与本地排序也适用于朵朵帖子。

## 使用边界

**本地运行版本**：当前验证阶段由单个用户在自己的设备上运行，并使用本人的论坛访问权限；它是发布前的实现阶段，不是产品的目标用户边界。
_避免使用_：个人本地工具

**本地个人背景**：用户主动提供并保存在本机的入学年份、专业或培养方向，用于解释依赖年级和学期的查询；不包含姓名、学号或认证信息。

**浏览器增强器**：运行在 CC98 原页面中的轻量工具，通过悬浮入口提供自然语言搜索；它不替代 CC98 的登录、浏览和互动界面。

**CC98 登录态**：用户已经在 CC98 页面取得的访问授权。浏览器增强器复用该授权，不另设账号、密码或 token 输入入口。

## 内容对象

**主题帖**：CC98 中由一个主楼和后续讨论组成的讨论单元，也是搜索结果跳转的基本对象。

**回帖**：主题帖中的楼层回复，包含对主楼或其他楼层的补充、回答、资料线索和讨论内容。

**可访问内容**：当前用户通过 CC98 官方授权能够读取的主题帖和回帖。

**候选主题帖**：来源平台的搜索接口根据检索词返回的主题帖；当前路线不使用回帖来发现主题帖。

**召回观察**：某个检索词在某一轮命中候选的事实，记录轮次、检索词和来源提供的完整结果流位次。候选可有多次观察，每次对应的排序文档均保留。

**独立检索证据**：足以让系统重新判断候选相关性的新信息，包括被不同检索词再次命中，或原生标题、发布时间、板块、回复数等允许模型判断的元数据发生变化。作者只留在本地，作者变化不构成独立检索证据；同一检索词下只有位次变化也不构成独立检索证据。

**检索支持**：候选被哪些不同检索词召回的关系。反馈轮可以接收有限数量的检索支持，但不接收来源位次、轮次、平台标识或原帖地址。

**排序文档**：来源提供给本地重排的标题、板块、时间等元数据及可选命中片段。片段仅供本地匹配，不进入模型反馈。

**标题来源**：区分平台原生标题与正文派生的显示标题。正文派生标题可以在结果卡片展示，但反馈给模型的标题为空，不因字段名称为“标题”而获得正文外发许可。

**主题证据**：候选主题帖的标题、元数据和按需读取的主楼内容，用于改善本地排序；搜索阶段不批量读取全部回帖。

## 搜索行为

**查询时检索**：用户发起搜索时，系统即时向 CC98 请求候选主题帖，并使用主题证据进行匹配排序；系统不承诺拥有或保存 CC98 的全站副本。

**迭代搜索**：系统在站点检索请求次数上限内执行有界的搜索循环。系统按检索波次收集候选，再让模型判断相关性并建议下一轮检索词；循环直到已知检索词的分页耗尽、没有可执行请求或请求次数上限耗尽。_避免使用_：一次性规划

**检索波次**：广度优先执行的一组站点检索请求。同一检索词在一个波次中最多请求一页；波次产生可判断的新证据后进入反馈轮。

**反馈轮**：检索波次结束后，模型对选中的主题证据给出相关性等级，并建议下一轮检索词和是否停止扩展。没有候选时，反馈轮也可以只根据原始查询提出救援检索词。输入包含自然语言查询、已执行检索词及其命中情况、有限的检索支持、原生标题、发布时间、板块和回复数，不包含作者、主楼正文、回帖、来源位次、平台标识、原帖地址、认证数据或用户身份。_避免使用_：重排请求

**反馈信号状态**：当前搜索运行中可用候选的整体相关性状态。无候选表示没有时间范围内的候选；弱信号表示存在候选但当前没有相关性等级 2 或 3；正信号表示当前至少存在一个等级 2 或 3 的候选。

**无正信号救援**：搜索处于无候选或弱信号状态时，反馈轮仅根据原始查询放宽表达并追加检索词。只有至少一个合法且未执行过的救援检索词进入队列时，运行才算使用了救援。一次搜索运行最多使用一次；正信号在救援前消失时，运行仍可以使用这次救援。_避免使用_：首轮盲扩展

**相关性等级**：反馈轮对候选主题帖给出的语义判断，`3` 表示明确高度相关，`2` 表示大概率相关，`1` 表示信息不足、部分相关或不确定，`0` 表示明确无关。只有 `0` 可以从主结果和后续反馈证据中排除；`1` 必须保留。

**反馈证据选择**：本地从待判断候选中选出有限且有代表性的主题证据交给反馈轮，避免宽泛检索词的候选独占模型输入。用户可以调整单次选择数量，字节上限仍由系统固定。

**软隔离候选**：被判断为明确无关的候选在当前结果和反馈证据中暂时隐藏，但仍保存在搜索运行内部；只有后续出现独立检索证据时，候选才能重新进入判断。

**查询依据扩展**：反馈轮仅根据原始自然语言查询提出的同义语、上位词、缩短表达或拆分概念。它不需要正信号支持，但只能用于一次尚未消耗的无正信号救援。

**证据依据扩展**：反馈轮从本轮候选的原生标题中学到的新检索词。每个检索词必须引用至少一个本轮相关性等级为 2 或 3 的候选键，所有引用都通过本地校验后才能执行。板块和回复数可以帮助判断相关性，但不作为新检索词的词汇来源。

**后续检索词**：模型在反馈轮中通过 `new_searches` 提出的查询依据扩展或证据依据扩展。系统逐条校验后才追加到待执行队列，一条无效扩展不影响同批其他扩展。反馈轮不能改写首轮确定的必须概念、排除词和时间约束。

**模型停止建议**：反馈轮认为不必再产生扩展词的建议。无候选或弱信号状态且救援尚未使用、仍有检索词尚未首次执行，或本轮已产生合法的首见检索词时，系统不接受该建议。一旦接受，本次运行不再追加扩展词，但仍完成已知分页，并继续通过反馈轮判断新候选的相关性。

**未命中检索词**：已经向 CC98 执行、且累计命中数为零的检索词。界面只用它说明检索结果，不把它表示成用户维护的停用词表。

**首次命中轮次**：候选主题帖第一次被某个检索词召回时所处的迭代轮次，用于本地重排的次序参考和界面说明。

**自然语言查询**：用户用日常语言描述一个可识别的论坛信息需求，可以包含时间、对象、板块、资料类型或排除条件；它不包括预测、纯主观决策或没有明确主题的请求。

**自然语言搜索**：系统将自然语言查询改写为多个检索词，依次向 CC98 召回候选主题帖，并在本地排序。结果用于发现原帖，不承诺穷尽 CC98 内容。
_避免使用_：语义搜索、调研式搜索、全量搜索、完整搜索

**查询改写**：模型把自然语言查询转换成按预期检索价值排序、可直接提交给 CC98 的完整检索词。浏览器不再组合或扩展这些检索词。

**查询计划**：查询规划器根据自然语言查询生成的临时结构。首轮包含查询组合、必须概念及其表达、排除词和时间约束；后续反馈轮只追加检索词，不再修改约束字段。浏览器先规范化模型返回的结构，再校验可用检索词、查询角色与时间范围。

**查询组合**：首轮同时使用不同颗粒度的检索词，避免把召回成败压在一种表达上。仅匹配标题的来源至少包含一个宽锚点和一个非宽锚点检索词；不设固定总数。模型未给全两类角色时，系统仍执行已有计划并提示组合不完整。

**查询角色**：查询组合中检索词的颗粒度。精确词组合多个意图约束，平衡词保留部分约束，宽锚点只保留用户意图中稳定的核心实体或概念。“求助”“经验”“帖子”“有没有”等通用词不是宽锚点。查询角色只用于在请求次数受限时为宽锚点和非宽锚点各保留一次首页机会，不影响候选排序。

**检索词**：查询规划器为来源平台生成的搜索表达。查询计划按预期检索价值排列检索词，系统依次执行并合并候选主题帖。

**检索词上限**：用户为一次自然语言搜索设置的可选约束。用户未设置时，系统不自行补充固定数量上限。

**站点检索请求次数上限**：用户允许一次自然语言搜索发出的站点 search 调用次数（默认 30，范围 1–100），分页请求同样计数。模型调用不计入；每次模型调用有独立的 20 秒固定超时。上限至少为 3 且首轮检索词可能耗尽全部请求时，系统在保证一个宽锚点和一个非宽锚点各执行一次后，为可能的无正信号救援预留一次请求；出现正信号或不再需要救援时释放该预留。系统到达请求次数上限，或遇到认证、权限、限流和网络错误时，停止继续请求并保留已经取得的候选主题帖。_避免使用_：检索时长、搜索总时长、模型预算

**模型查询规划**：第三方模型根据自然语言查询、浏览器当前日期和人工维护的论坛术语生成首轮查询组合；在反馈轮中再根据原始查询或新增主题证据生成后续检索词。所有规划调用都关闭模型思考并限制输出长度，每次最多等待 20 秒。模型接收主题帖的原生标题、发布时间、板块和回复数；不接收作者、主楼正文、回帖内容、论坛账号或认证数据。纯单关键词的模型计划无法恢复时，浏览器直接搜索用户原词并显示提示；模型超时和网络错误不使用该兜底。

**当前查询计划**：当前搜索运行使用的临时计划，保存在页面内存中。再次提交查询会发起新运行；不缓存或复用已结束运行的结果。

**重新规划**：为自然语言查询重新开始迭代搜索，清空反馈历史和候选集合并回到第一轮。当前版本通过再次提交查询开始新运行，不提供单独的“换一组检索词”入口。

**搜索运行**：执行一个当前查询计划并持续合并、重排候选主题帖的过程。用户关闭悬浮入口不会终止运行；用户停止、发起新查询或离开当前 CC98 页面时终止运行。

**广度优先检索**：系统按查询计划顺序让每个检索词先取得一页候选主题帖。未达站点检索请求次数上限时，系统再为返回非空页的检索词继续分页，直到返回空页或命中其他本地停止条件。模型停止建议不中断已知分页。

**部分结果**：搜索运行提前终止时已经取得并完成本地排序的候选主题帖。界面必须说明终止原因，不能把部分结果表示为完整结果。

**本地预排序**：浏览器先执行明确时间硬约束，确定越界的候选不进入主结果、软隔离区域或反馈证据，发布时间未知的候选保留；用户没有提出时间条件时不执行时间过滤。通过时间约束的候选先比较相关性等级，再比较排除词、必须概念、跨检索命中、来源原始位次和首次命中轮次。软隔离候选不进入主结果，在获得独立检索证据前也不进入反馈证据。模型阶段关闭、取消或失败时，结果完整保留这个确定性顺序。_避免使用_：本地重排

**相关结果**：能够满足自然语言查询中主要意图和约束的主题帖，结果必须保留原帖跳转地址。

**搜索结果列表**：一次自然语言搜索返回的临时主题帖列表；列表本身不进入长期收藏。

**搜索结果卡片**：搜索结果列表中一个主题帖的展示单元，只显示标题、作者、板块、发布时间、回复数和原帖入口。当前产品不显示内部得分、排序原因、时间状态、检索词命中数或实验指标。


**最终列表重排**：搜索运行结束后，模型只对本地预排序前 M 条主结果候选做一次相对排序。板块可以帮助判断查询意图；回复数只是其他相关性信号接近时的弱破同分信号，不能单独提高相关性等级。相关性等级 1、2 和 3 只能排序，只有请求发出前仍未判断且被模型认定为明确无关的候选可以移除。相关性等级 0、时间范围外候选和 Top-M 外候选不进入调用。用户可以关闭、限制候选数量或取消正在执行的重排；关闭、跳过、取消或失败时保留本地预排序。_避免使用_：最终意图筛选

**来源排序能力**：搜索来源对结果顺序的保证。CC98 保证按发布时间倒序，朵朵按热度排序；系统只在前者上利用时间顺序减少无效翻页。

**时间提前停止**：对于可靠地按发布时间倒序的来源，整页结果均已早于查询起始日期时停止该检索词的后续分页。未知发布时间、空页或仍在范围内的结果不会触发提前停止；其他检索词继续运行并共享同一请求次数上限。

## 本地资料管理

**收藏库**：用户主动保存的主题帖集合，使用可嵌套文件夹组织，并永久保存在本地，直到用户明确删除。

**收藏夹**：收藏库中用于组织收藏项的容器；收藏夹可以包含子收藏夹。

**收藏项**：收藏库中指向一个主题帖的唯一记录；用户收藏时选择一个或多个收藏夹，其阅读状态和用户笔记在收藏夹之间共享。

**浏览记录**：用户首次同意后，由浏览器增强器自动记录的主题帖访问轨迹；记录只包含主题帖标识、标题、链接、访问时间和访问次数，不包含帖子正文。

**搜索记录**：用户执行过的自然语言查询及其发生时间；它用于重新发起查询，不等同于搜索结果列表。
