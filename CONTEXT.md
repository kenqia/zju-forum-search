# CC98 自然语言搜索

本项目面向浙江大学学生，帮助用户在本人可访问的 CC98 内容范围内，用自然语言定位相关主题帖。

## 使用边界

**本地运行版本**：当前验证阶段由单个用户在自己的设备上运行，并使用本人的论坛访问权限；它是发布前的实现阶段，不是产品的目标用户边界。
_避免使用_：个人本地工具

**本地个人背景**：用户主动提供并保存在本机的入学年份、专业或培养方向，用于解释依赖年级和学期的查询；不包含姓名、学号或认证信息。

**浏览器增强器**：运行在 CC98 原页面中的轻量工具，通过悬浮入口提供自然语言搜索；它不替代 CC98 的登录、浏览和互动界面。

**CC98 登录态**：用户已经在 CC98 页面取得的访问授权。浏览器增强器复用该授权，不另设账号、密码或 token 输入入口。

## 内容对象

**主题帖**：CC98 中由一个主楼和后续讨论组成的讨论单元，也是搜索结果跳转的基本对象。

**回帖**：主题帖中的楼层回复，包含对主楼或其他楼层的补充、回答、资料线索和讨论内容。

**可访问内容**：当前用户通过 CC98 官方授权能够读取的主题帖和回帖。

**候选主题帖**：来源平台的主题搜索接口根据检索词返回的主题帖；当前路线不使用回帖来发现主题帖。

**主题证据**：候选主题帖的标题、元数据和按需读取的主楼内容，用于改善本地排序；搜索阶段不批量读取全部回帖。

## 搜索行为

**查询时检索**：用户发起搜索时，系统即时向 CC98 请求候选主题帖，并使用主题证据进行匹配排序；系统不承诺拥有或保存 CC98 的全站副本。

**迭代搜索**：系统在 CC98 检索预算内执行有界的搜索循环。每一轮先向 CC98 发送当轮检索词并收集候选，再让模型阅读本轮新增的主题证据，学习下一轮可用的检索词；循环直到模型判断停止、无新候选或预算耗尽。_避免使用_：一次性规划

**反馈轮**：迭代搜索中第二轮及以后的规划调用。模型输入包含自然语言查询、已执行检索词及其命中情况、本轮新增候选的主题证据（标题、作者、发布时间、板块、回复数）；不包含主楼正文、回帖、认证数据或用户身份。_避免使用_：重排请求

**学习到的检索词**：模型在反馈轮中根据已见主题证据提出的新检索词，追加到待执行队列；模型可以同时建议停用已被证明无命中或偏离目标的检索词，但不能改写首轮确定的必须概念、排除词和时间约束。

**首轮盲扩展**：第一轮全部检索词返回零候选时，模型在没有主题证据的情况下放宽约束、换用更宽的同义表达再执行一轮；仍无候选则按“没有找到主题帖”收尾。

**首次命中轮次**：候选主题帖第一次被某个检索词召回时所处的迭代轮次，用于本地重排的次序参考和界面说明。

**自然语言查询**：用户用日常语言描述一个可识别的论坛信息需求，可以包含时间、对象、板块、资料类型或排除条件；它不包括预测、纯主观决策或没有明确主题的请求。

**自然语言搜索**：系统将自然语言查询改写为多个检索词，依次向 CC98 召回候选主题帖，并在本地排序。结果用于发现原帖，不承诺穷尽 CC98 内容。
_避免使用_：语义搜索、调研式搜索、全量搜索、完整搜索

**查询改写**：模型把自然语言查询转换成按预期检索价值排序、可直接提交给 CC98 的完整检索词。浏览器不再组合或扩展这些检索词。

**查询计划**：查询规划器根据自然语言查询生成的临时结构。首轮包含按预期检索价值排序的检索词、必须概念及其表达、排除词和时间约束；后续反馈轮只追加或停用检索词，不再修改约束字段。

**检索词**：查询规划器为来源平台生成的搜索表达。查询计划按预期检索价值排列检索词，系统依次执行并合并候选主题帖。

**检索词上限**：用户为一次自然语言搜索设置的可选约束。用户未设置时，系统不自行补充固定数量上限。

**CC98 检索预算**：用户允许一次自然语言搜索用于 CC98 请求和请求间隔的时间。模型等待不消耗这段预算；每次模型调用有独立的 20 秒固定超时。系统到达检索预算，或遇到认证、权限、限流和网络错误时，停止继续请求并保留已经取得的候选主题帖。_避免使用_：模型预算、搜索总时长

**模型查询规划**：第三方模型根据自然语言查询、浏览器当前日期和人工维护的论坛术语生成首轮检索词；在反馈轮中再根据新增主题证据学习后续检索词。所有规划调用都关闭模型思考并限制输出长度，每次最多等待 20 秒。模型接收主题帖标题、作者、发布时间、板块和回复数等元数据；不接收主楼正文、回帖内容、论坛账号或认证数据。

**当前查询计划**：与规范化查询、检索词上限、术语表版本和规划器版本对应的查询计划。同一组输入在当前 CC98 页面内复用该计划，刷新或关闭页面后清除。

**重新规划**：用户明确要求为当前自然语言查询重新开始迭代搜索，清空全部反馈历史和候选集合并回到第一轮。网络重试和继续搜索不属于重新规划。

**搜索运行**：执行一个当前查询计划并持续合并、重排候选主题帖的过程。用户关闭悬浮入口不会终止运行；用户停止、发起新查询或离开当前 CC98 页面时终止运行。

**广度优先检索**：系统按查询计划顺序让每个检索词先取得一页候选主题帖。仍有 CC98 检索预算时，系统再为返回满页的检索词继续分页。

**部分结果**：搜索运行提前终止时已经取得并完成本地排序的候选主题帖。界面必须说明终止原因，不能把部分结果表示为完整结果。

**本地重排**：浏览器根据查询计划和候选主题证据调整顺序。用户明确提出年份或相对时间范围时，浏览器先校验模型返回的起止日期，再从主排行榜和反馈证据中移除确定在范围外的帖子，并显示忽略数量；发布时间未知的帖子保留。用户没有提出时间条件时不执行过滤。其余候选再比较排除词、必须概念、跨检索命中和 CC98 原始排名；召回与重排分别验收。

**相关结果**：能够满足自然语言查询中主要意图和约束的主题帖，结果必须保留原帖跳转地址。

**搜索结果列表**：一次自然语言搜索返回的临时主题帖列表；列表本身不进入长期收藏。

**搜索结果卡片**：搜索结果列表中一个主题帖的展示单元，只显示标题、板块、发布时间、回复数和原帖入口。当前产品不显示内部得分、排序原因、时间状态、检索词命中数或实验指标。

## 本地资料管理

**收藏库**：用户主动保存的主题帖集合，使用可嵌套文件夹组织，并永久保存在本地，直到用户明确删除。

**收藏夹**：收藏库中用于组织收藏项的容器；收藏夹可以包含子收藏夹。

**收藏项**：收藏库中指向一个主题帖的唯一记录；用户收藏时选择一个或多个收藏夹，其阅读状态和用户笔记在收藏夹之间共享。

**浏览记录**：用户首次同意后，由浏览器增强器自动记录的主题帖访问轨迹；记录只包含主题帖标识、标题、链接、访问时间和访问次数，不包含帖子正文。

**搜索记录**：用户执行过的自然语言查询及其发生时间；它用于重新发起查询，不等同于搜索结果列表。
