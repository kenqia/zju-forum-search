# #12–19 交付复核

日期：2026-09-18。代码复核范围：`git diff 229ce94...3934d51`，另含本轮公开调查记录和验收文档更新。

## 结论

规范审查未发现实质缺陷；需求审查未发现新增运行时缺陷。两路审查分别核对源码与需求，不将测试存在当作真实站点已验收的证明。

用户已勾选 #13–16 的验收项，运行朵朵向导后反馈“整体挺好”，并明确接受风控阈值不阻塞交付。用户授权检查通过后 commit、push 和关闭本批 issue。

| Issue | 交付结论 |
| --- | --- |
| #13 | SearchSource 端口、错误与游标契约已落地。 |
| #14 | CC98 认证、传输、分页与映射已隔离至 adapter；以该票原提交的 manifest 不变为验收范围。 |
| #15 | 候选三层模型、来源策略、搜索预算及本地重排已落地。 |
| #16 | 能力提示词、core 反馈白名单与文件拆分完成。 |
| #17 | registry 与 manifest 模式并集覆盖测试通过，允许额外模型权限。 |
| #18 | 朵朵 adapter 完成，接入提交没有修改 SearchSession、ranking 或 planner；保留下面的文件范围说明。 |
| #19 | 可执行向导、确认门与交互覆盖完成，用户给出使用反馈；专业未决项保留为研究限制，不宣称逐项实测完成。 |
| #12 | 两个来源通过同一搜索端口运行，总需求按上述边界交付。 |

## 需要保留的边界

#18 除 adapter、registry、manifest 外，还修复了后台仅接受 CC98 的来源限制及 UI 专属文案，并增加配套测试与文档。否则朵朵页面无法调用模型。该必要改动已记录在 ADR-0005，不满足“diff 只限三个位置”的字面要求，但核心零修改且没有按朵朵 ID 打补丁。

#19 的 ddtk 与 duo_session 实际值关系、未登录空结果原因、needCode 触发阈值及服务器跨校访问范围没有全部测定。公开源码已核对认证链路与“只看本校”的请求形态，单次未登录 RPC 已验证加密互操作。用户接受阈值不阻塞交付，不等于阈值已知。关闭说明保留这些限制，不把未确认框改成已验证。

## 新鲜验证

- `cd frontend && npm test -- --maxWorkers=2`：12 个文件，124 项通过。
- `cd frontend && npm run build`：类型检查、两个生产 bundle 与扩展产物检查通过。
- `bash -n frontend/scripts/issue-14-15-acceptance-wizard.sh`：通过。
- `bash -n frontend/scripts/issue-18-19-acceptance-wizard.sh`：通过。
- `git diff --check`：通过。

本轮没有重新访问真实登录态，没有压测站点或尝试触发风控。CC98 手动验收向导随本次提交纳入版本控制，更新了现有“搜”入口名称；专业调查证据见 `docs/research/duo-alumni-api-survey.md`。
